# Core-Python-Project-
A single, menu-driven console application that bundles all your Core Python mini-projects into one program. Each menu option is its own module/section, so you can study, run, or extend them independently — but they all live under one roof (main.py).
